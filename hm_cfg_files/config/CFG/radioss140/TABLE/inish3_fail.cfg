// --- inish3_fail.cfg ---

ATTRIBUTES(COMMON)
{
    // INPUT ATTRIBUTES
    inish3_fail_count                  = SIZE("inish3_fail_count");
    INISH3_FAIL_SUBOBJ                 = ARRAY[inish3_fail_count](SUBOBJECT, "List of keys/items");

    // HM INTERNAL
    KEYWORD_STR                        = VALUE(STRING, "List of keys/items");
}

SKEYWORDS_IDENTIFIER(COMMON)
{
    //INPUT ATTRIBUTES
    inish3_fail_count                  = -1;
    INISH3_FAIL_SUBOBJ                 = -1;

    //HM INTERNAL
    KEYWORD_STR                        = 9000;
}


GUI(COMMON)
{
    ASSIGN(KEYWORD_STR, "/INISH3/FAIL/");

    SUBOBJECT(INISH3_FAIL_SUBOBJ)      { SUBTYPES = ( /SUBOBJECT/INISH3_FAIL_SUBOBJ ) ; }
}

// File format
FORMAT(radioss140)
{
    HEADER("/INISH3/FAIL/");
    SUBOBJECTS(INISH3_FAIL_SUBOBJ, /SUBOBJECT/INISH3_FAIL_SUBOBJ);
}
