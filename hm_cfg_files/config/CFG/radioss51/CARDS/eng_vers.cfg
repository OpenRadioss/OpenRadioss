//
// /VERS
//

ATTRIBUTES(COMMON)
{
    //INPUT ATTRIBUTES
    ENG_VERS_VersNum                        = VALUE(INT, "Version Number", "VersionNumber");

    // HM INTERNAL
    KEYWORD_STR                             = VALUE(STRING, "Solver Keyword");
}

SKEYWORDS_IDENTIFIER(COMMON)
{
    //INPUT ATTRIBUTES
    ENG_VERS_VersNum                        = 6165;

    // HM INTERNAL
    KEYWORD_STR                             = 9000;
}

GUI(COMMON)
{
}

// File format
FORMAT(radioss44)
{
    HEADER("/VERS/%d",ENG_VERS_VersNum);
}
FORMAT(radioss51)
{
    HEADER("/VERS/%d",ENG_VERS_VersNum);
}