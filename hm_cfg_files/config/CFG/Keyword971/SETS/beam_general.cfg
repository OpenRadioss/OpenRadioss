//
// Group Setup File
// *SET_BEAM_GENERAL
// 

// MCDS attributes description
ATTRIBUTES {
  // Access to nodes
    clausesmax                  = SIZE("Numbers of keys/items");    
    clauses                     = ARRAY[clausesmax](SUBOBJECT, "List of keys/items");
//
    LSD_COLLECT_OPT             = VALUE(INT, "Flag to set COLLECT");
//
    TITLE                       = VALUE(STRING, "TITLE");
    LSD_TitleOpt                = VALUE(INT, "Title");

// HM INTERNAL
    set_Type                    = VALUE(STRING, "Set Type");
    option_Type                 = VALUE(STRING, "Set Type");
    KEY                         = VALUE(STRING, "header Type");
 /*   KEYWORD_STR               = VALUE(STRING, "Solver Keyword");*/
    NUM_COMMENTS                = SIZE("NUM_COMMENTS");
    CommentEnumField            = VALUE(INT,"User Comments");
    COMMENTS                    = ARRAY[NUM_COMMENTS](STRING,"Entity Comments");
    _IDPOOL                     = VALUE(INT, "Set Element Id pool");
    IO_FLAG                     = VALUE(INT, "Import/Export flag");
}
SKEYWORDS_IDENTIFIER(COMMON)
{   
 /*   KEYWORD_STR               = 9000;*/
    COMMENTS                    = 5109;
    CommentEnumField            = 7951;
    NUM_COMMENTS                = 5110;
    LSD_TitleOpt                = 90;
    set_Type                    = -1;
    option_Type                 = -1;
    IO_FLAG                     = -1;
    LSD_COLLECT_OPT             = 9001;
    KEY                         = -1;
}
DEFAULTS(COMMON)
{
  _IDPOOL                       = 42;
  set_Type                      = "GENERAL";
  LSD_TitleOpt                  = 0;
  LSD_COLLECT_OPT               = 0;
  KEY                           = "*SET_BEAM_GENERAL";
}

GUI(COMMON)
{
mandatory:
   SIZE(clausesmax) ;
   ARRAY(clausesmax,"")
   {
        SUBOBJECT(clauses) {SUBTYPES = (/SUBOBJECT/SET_BEAM_GENERAL_SUPPORT);}     
   }
optional: 
  FLAG(LSD_TitleOpt);
  FLAG(LSD_COLLECT_OPT);
  
}
FORMAT(Keyword971) {
  ASSIGN(IO_FLAG, 1, IMPORT);
  ASSIGN(IO_FLAG, 0, EXPORT);
//import header and option
  if(IO_FLAG == 1)
  {
     HEADER("*SET_BEAM_GENERAL%s", option_Type);
  }
  ASSIGN(LSD_TitleOpt, _FIND(option_Type, "TITLE"),IMPORT);
  ASSIGN(LSD_COLLECT_OPT, _FIND(option_Type, "COLLECT"),IMPORT);
//
//export header
  ASSIGN(KEY,*SET_BEAM_GENERAL, EXPORT);
  if(IO_FLAG == 0 && LSD_COLLECT_OPT == 1)
  {
      ASSIGN(KEY,_COMBINE(KEY,"_COLLECT"), EXPORT);
  }
  if(IO_FLAG == 0 && LSD_TitleOpt == 1)
  {
      ASSIGN(KEY,_COMBINE(KEY,"_TITLE"), EXPORT);
  }
  if(IO_FLAG == 0)
  {
      HEADER("%s",KEY);
  }
//
  if(IO_FLAG == 0 && LSD_TitleOpt == 1)  
  {
        CARD("%-80s", TITLE);
  }
  COMMENT("$      SID");
  CARD("%10d",_ID_);
  COMMENT("$   OPTION         E         E         E         E         E         E         E");
  SUBOBJECTS(clauses,/SUBOBJECT/SET_BEAM_GENERAL_SUPPORT);   
}
