//
// /INIBRI: /INIBRI/ORTHO
//

ATTRIBUTES(COMMON)
{
    // INPUT ATTRIBUTES
    inibri_ortho_count                        = SIZE("");
    INIBRI_ORTHO_SUBOBJ                       = ARRAY[inibri_ortho_count](SUBOBJECT, "List of keys/items");

    //HM INTERNAL
    KEYWORD_STR                               = VALUE(STRING, "Solver Keyword");
}

SKEYWORDS_IDENTIFIER(COMMON)
{

    //INPUT ATTRIBUTES
    inibri_ortho_count                        = -1;
    INIBRI_ORTHO_SUBOBJ                       = -1;

    //HM INTERNAL
    KEYWORD_STR                               = 9000;
}

CHECK(COMMON)
{

}

DEFAULTS(COMMON)
{

}

GUI(COMMON)
{
    ASSIGN(KEYWORD_STR, "/INIBRI/ORTHO");

    SUBOBJECT(INIBRI_ORTHO_SUBOBJ)            {SUBTYPES = (/SUBOBJECT/INIBRI_ORTHO_SUBOBJ);}
}

// File format
FORMAT(radioss110)
{
    HEADER("/INIBRI/ORTHO");

    SUBOBJECTS(INIBRI_ORTHO_SUBOBJ,/SUBOBJECT/INIBRI_ORTHO_SUBOBJ);
}
