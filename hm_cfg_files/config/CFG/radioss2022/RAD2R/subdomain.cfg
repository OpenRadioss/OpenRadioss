//
// Slipring Setup File
// 

ATTRIBUTES {
  // Single values
  idsmax         = SIZE("Number of COMPONENT");
  ids            = ARRAY[idsmax](INT, "Identifiers of the parts of the subdomain");
  displayname    = VALUE(STRING, "Title");
}

DEFAULTS {
}

GUI(COMMON)
{
    SIZE(idsmax);
}


FORMAT(radioss110) {
  HEADER("/SUBDOMAIN/%d",_ID_);
  CARD("%-100s", displayname);

  COMMENT("#      Obj       Obj       Obj       Obj       Obj       Obj       Obj       Obj       Obj       Obj");
  FREE_CELL_LIST(idsmax,"%10d",ids,100);
}
